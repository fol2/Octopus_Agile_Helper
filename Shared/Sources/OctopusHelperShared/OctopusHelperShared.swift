// This is the main entry point for the OctopusHelperShared module
@_exported import Foundation

// Re-export all public types from the module
public struct OctopusHelperShared {
    // This is a namespace struct that serves as the module's entry point
    // All types are already marked as public in their respective files
} 